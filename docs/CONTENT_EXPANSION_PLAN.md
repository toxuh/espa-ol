# План развития учебного контента

Обновлено: 20 июля 2026 года.

## Назначение документа

Этот документ — handoff для отдельной сессии, посвящённой только учебному
контенту и инфраструктуре его seed-загрузки. Он фиксирует текущее состояние
банка, найденные дефекты, целевую организацию файлов и порядок расширения.

Главная цель — превратить механически перенесённый контент HTML-прототипа в
поддерживаемый учебный каталог, который можно безопасно вычитывать, расширять и
загружать в PostgreSQL без потери стабильности существующих учебных дней.

В рамках этой работы не меняются визуальный дизайн, дневная нагрузка,
адаптивный алгоритм и продуктовые правила первой попытки, если изменение не
нужно непосредственно для отображения более полного контента.

## Текущее состояние

В модульном каталоге `src/content/levels/<level>/<kind>.ts` находится 335
seed-элементов (A1 расширен по короткоротационным разделам, см. этап 4):

| Раздел        | Всего | Распределение по уровню    |
| ------------- | ----: | -------------------------- |
| Грамматика    |   102 | по 20 для A1–B2, 22 для C1 |
| Теория        |    30 | по 6                       |
| Словарь       |    70 | по 14                      |
| Спряжения     |    54 | 30 для A1, по 6 для A2–C1  |
| Чтение        |    20 | 8 для A1, по 3 для A2–C1   |
| Аудирование   |    16 | 8 для A1, по 2 для A2–C1   |
| Перевод ES→RU |    14 | 6 для A1, по 2 для A2–C1   |
| Перевод RU→ES |    14 | 6 для A1, по 2 для A2–C1   |
| Placement     |    15 | по 3                       |

Все 335 элементов имеют явные уникальные ID; 15 placement-ID были перенесены из
прежнего правила генерации по позиции и теперь зафиксированы в каталоге.

### Что уже является хорошей основой

- Уровни A1–C1 заполнены равномерно.
- Все грамматические упражнения имеют объяснение.
- MCQ и fill-in распределены почти поровну.
- Теоретические уроки не являются заглушками: в них есть правила, формы и
  примеры; текущий объём — примерно 114–168 слов на урок.
- Длина чтения и переводов постепенно увеличивается от A1 к C1.
- Стабильные ID сохранены из прототипа.
- Seed идемпотентно обновляет записи по `sourceId` и деактивирует отсутствующий
  контент вместо физического удаления.
- Банк уже покрывает полный пользовательский цикл MVP.

Это хороший parity-базис, но не достаточный каталог для длительного обучения.

## Найденные проблемы

### P0. Ошибки и нарушения целостности

#### Неправильные канонические ответы

Шесть ответов хранятся без обязательного графического ударения:

| ID    | Сейчас           | Должно быть      |
| ----- | ---------------- | ---------------- |
| a1-12 | `lapices`        | `lápices`        |
| a2-01 | `compre`         | `compré`         |
| b1-04 | `hablaria`       | `hablaría`       |
| b2-09 | `habia empezado` | `había empezado` |
| b2-13 | `queria`         | `quería`         |
| b2-14 | `cual`           | `cuál`           |

Нормализация ответа должна по-прежнему принимать ввод без диакритики, но
канонический ответ, объяснение и показанный пользователю эталон обязаны быть
написаны правильно.

#### Слово не выделяется в контексте

Компонент ищет точное вхождение `word`, но в семи контекстах используется
изменённая форма:

- `v-b2-1`: exigir → exigen;
- `v-b2-3`: desempeñar → desempeña;
- `v-b2-4`: abarcar → abarca;
- `v-b2-5`: plantear → plantea;
- `v-b2-7`: carecer → carece;
- `v-b2-8`: suscitar → suscitó;
- `v-c1-7`: arraigado → arraigada.

Нужно добавить явное поле `contextTarget` и валидировать, что именно оно
присутствует в `context`. Поле `word` остаётся словарной формой.

#### Другие точечные дефекты

- `b2-05` неоднозначен: в текущем предложении допустимы как `Estudiando
mucho`, так и `Al estudiar mucho`.
- В `th-a2-6` ошибочно написано «сравнение
  превосходства/уступительности».
- Объяснение `ser/estar` через абсолютное «постоянное/временное» слишком
  категорично. Его нужно оставить как начальную подсказку, но дополнить более
  точной моделью: идентификация/характеристика против состояния/местоположения,
  с контрастными примерами и исключениями.
- Автоматический пример в теории заменяет только первый `___` и некорректен для
  заданий с несколькими пропусками. Для карточки нужен явный `solvedExample`.

### P1. Короткая ротация

Текущий банк исчерпывается слишком быстро относительно дневной нагрузки:

- чтение повторяется после трёх дней на уровне;
- каждый вид перевода — после двух дней;
- теория — после шести уроков;
- на уровне есть ровно два listening-элемента, поэтому почти один и тот же
  набор показывается каждый день;
- доступных спряжений меньше дневного лимита 20, поэтому генератор практически
  ежедневно возвращает весь допустимый пул;
- грамматика на краевых уровнях начинает заметно повторяться через 2–3 дня.

Адаптивное повторение является частью продукта, но оно должно возвращать
слабый материал осознанно, а не потому, что нового материала больше нет.

### P1. Недостаточная учебная глубина

#### Грамматика

Текущее `explain` обычно содержит одно правило и один пример. Для полноценной
обратной связи нужны:

- короткая подсказка до ответа;
- объяснение выбора формы в конкретном предложении;
- контраст с вероятной ошибкой;
- дополнительные примеры;
- типичная ошибка русскоязычного ученика;
- явная связь с уроком теории;
- отдельные допустимые ответы и один канонический ответ.

#### Теория

Каждый урок должен иметь одинаковую педагогическую структуру:

1. цель урока;
2. краткая интуиция;
3. полное правило или схема;
4. таблица форм, если применимо;
5. 4–6 примеров с переводом;
6. контрастные примеры;
7. типичные ошибки;
8. краткая шпаргалка;
9. ссылки на упражнения закрепления.

Сейчас восемь упражнений по шести темам не связаны ни с одним уроком:

- `b1-10`, `b1-13`, `b1-14`;
- `b2-13`;
- `c1-09`, `c1-11`, `c1-17`, `c1-19`.

#### Словарь

Нужно добавить часть речи, род существительных, значимые формы, управление,
сочетания, форму слова в контексте, перевод контекста и заметку о регистре или
употреблении.

#### Спряжения

Нужны правило образования, неправильная основа или изменение корня, временные
маркеры, минимум два примера и типичная ошибка. Шесть форм и один пример
остаются проверяемым ядром карточки.

#### Чтение

К каждому тексту нужны:

- краткое задание перед чтением;
- словарь из 4–10 выражений;
- 3–5 вопросов на понимание;
- ответы и короткое объяснение;
- при необходимости культурная или языковая заметка.

#### Переводы

Нужны несколько допустимых эталонов, ключевые конструкции, разбор сложных мест
и примечания о естественном порядке слов. Текущий word-overlap остаётся
приблизительной оценкой до отдельного продуктового решения.

#### Аудирование

Сейчас карточки ведут преимущественно на каналы или большие плейлисты. В десяти
карточках только семь уникальных URL, а некоторые ссылки повторяются между
уровнями. Для каждого элемента нужны:

- ссылка на конкретное видео или эпизод;
- длительность;
- вариант испанского и примерная скорость речи;
- инструкция, что именно слушать;
- 3–5 вопросов;
- transcript, полный или учебный фрагмент, если это разрешено источником;
- дата последней проверки ссылки.

#### Placement

Три вопроса на уровень недостаточны для устойчивой диагностики. Вопросам нужны
явные ID, `topic`, объяснение и отметка проверяемого навыка. Расширение банка и
изменение алгоритма выбора 15 вопросов должны выполняться отдельным
согласованным срезом, чтобы не изменить пороги незаметно.

### P1. Нет настоящей валидации seed-данных — закрыто этапом 1

Каталог теперь проходит Zod/runtime-валидацию до обращения к БД.
`npm run content:check` проверяет дополнительные межэлементные инварианты,
минимальные объёмы и manifest защищённых смысловых полей.

### P1. Изменение контента может переписать смысл старого дня

`DailyPlan` хранит source IDs, а при открытии приложение загружает актуальный
`ContentItem.data`. Если под существующим ID изменить prompt, ответ или смысл,
старый день покажет новую карточку рядом со старой попыткой.

Правило до реализации полноценной revision-модели:

- орфография, пунктуация и расширение объяснения без изменения проверяемого
  смысла могут сохранять ID;
- изменение prompt, правильного ответа, типа задания или проверяемого правила
  требует нового ID и деактивации старой карточки;
- `content:check` должен уметь обнаруживать изменение защищённых полей по
  сохранённому manifest/digest;
- позднее можно заменить это правило на явную ревизию в БД и ссылку дневного
  плана на конкретную ревизию.

## Целевая организация файлов

Прежний `prototype-content.json` имел около 2 750 строк и удалён из
канонического пути после проверенного структурного переноса.

Рекомендуемая структура:

```text
src/content/
  schemas.ts
  catalog.ts
  shared/
    levels.ts
    placement.ts
  levels/
    a1/
      grammar.ts
      theory.ts
      vocabulary.ts
      conjugations.ts
      readings.ts
      translations.ts
      listening.ts
    a2/
      ...
    b1/
      ...
    b2/
      ...
    c1/
      ...
```

Правила организации:

- основной размер дробления — один уровень и один вид контента;
- не создавать отдельный файл на каждое упражнение или слово;
- теорию делить по отдельным урокам только тогда, когда файл уровня становится
  действительно неудобным;
- `catalog.ts` должен явно импортировать и объединять все модули;
- seed импортирует проверенный каталог, а не читает монолитный JSON;
- `scripts/extract-prototype-content.mjs` остаётся архивным одноразовым
  инструментом, но больше не генерирует канонический каталог;
- порядок элементов должен быть детерминированным и не зависеть от glob или
  порядка обхода файловой системы.

TypeScript-модули предпочтительнее ручных JSON-файлов: они дают `satisfies`,
многострочные строки, комментарии и раннюю проверку типов. Zod всё равно нужен
для полной runtime-валидации перед seed.

## Целевые контракты контента

Названия полей можно уточнить при реализации, но обязательные возможности
фиксируются сейчас.

### GrammarExercise

- `id`, `revision`, `level`, `topic`, `lessonIds`;
- `type`, `prompt`, `options`;
- `answer`, `acceptedAnswers`;
- `hint`;
- `explain`;
- `solvedExample`;
- `examples[]`;
- `commonMistakes[]`.

### TheoryLesson

- `id`, `revision`, `level`, `title`, `topics`;
- `objectives[]`;
- структурированные `sections[]` вместо произвольной HTML-строки;
- `summary[]`;
- `examples[]` с испанским текстом и переводом;
- `commonMistakes[]`;
- `exerciseIds[]` или проверяемая обратная связь через topic registry.

### VocabularyCard

- `id`, `revision`, `level`;
- `word`, `partOfSpeech`, `gender`, `forms`;
- `translation`, `accept`;
- `context`, `contextTarget`, `contextTranslation`;
- `collocations[]`, `usageNote`, `register`.

### ConjugationCard

- текущие verb/tense/forms;
- `formationRule`, `irregularity`, `markers[]`;
- `examples[]` вместо одного примера;
- `commonMistakes[]`.

### ReadingContent

- текущие title/text;
- `instructions`, `glossary[]`, `questions[]`, `notes[]`;
- ответы на вопросы внутри trusted seed-контента.

### TranslationContent

- текущие text/reference;
- `alternativeReferences[]`;
- `keyConstructions[]`;
- `notes[]`;
- для RU→ES — список допустимых ключевых слов или выражений отдельно от полного
  эталона.

### ListeningContent

- текущие title/channel/url;
- `durationMinutes`, `dialect`, `speed`, `instructions`;
- `questions[]`, `transcript` или `summary`;
- `lastVerifiedAt`.

### PlacementQuestion

- явный `id`, `level`, `topic`, `skill`;
- текущие prompt/options/answer;
- `explanation`;
- позднее — данные калибровки, если появится достаточно попыток.

## Ориентиры объёма

Это первый целевой диапазон, а не требование заполнить всё одним коммитом:

| Раздел               | На уровень |
| -------------------- | ---------: |
| Грамматика           |      60–80 |
| Словарь              |      40–60 |
| Спряжения            |      20–30 |
| Теория               |      10–12 |
| Чтение               |       8–10 |
| Перевод каждого вида |        6–8 |
| Аудирование          |       8–10 |
| Placement            |        5–8 |

Приоритет расширения определяется не только числом элементов, но и горизонтом
повторения при реальной дневной нагрузке.

## План реализации

### Этап 1. Инфраструктура каталога

- [x] Добавить Zod-схемы всех типов контента.
- [x] Создать модульную структуру `levels/<level>/<kind>.ts`.
- [x] Перенести текущие 292 элемента без изменения поведения и ID.
- [x] Создать детерминированный `catalog.ts`.
- [x] Перевести Prisma seed на импорт каталога.
- [x] Добавить `npm run content:check`.
- [x] Добавить отчёт по количеству элементов, темам и горизонту повторения.
- [x] Обновить `DATA_MODEL.md` и описание seed-процесса.

### Этап 2. Исправление текущего банка

- [x] Исправить шесть канонических ответов с диакритикой.
- [x] Добавить `contextTarget` и исправить семь словарных карточек.
- [x] Устранить неоднозначность `b2-05`.
- [x] Исправить терминологию `th-a2-6`.
- [x] Переписать объяснение `ser/estar` без абсолютного правила
      «постоянное/временное».
- [x] Связать восемь orphan-упражнений с теорией.
- [x] Добавить явные solved examples для заданий с несколькими пропусками.
- [x] Выполнить полную языковую вычитку всех текущих элементов.

Этап 2 также исправил найденные при сквозной вычитке дефекты `b2-07`,
`c1-04`, `tf-a2-1` и `tf-b2-2`. Изменения защищённых полей получили новую
`revision`; manifest обновлён после проверки этого условия.

### Этап 3. Эталонный вертикальный срез A1

- [x] Расширить все шесть A1-уроков по новому шаблону.
- [x] Обогатить существующие A1 grammar/vocabulary/conjugation карточки.
- [x] Добавить задания на понимание к A1 readings.
- [x] Добавить разбор к обоим направлениям перевода.
- [x] Заменить общие listening-ссылки конкретными материалами и заданиями.
- [x] Проверить отображение всех новых полей в приложении.
- [x] Утвердить шаблон качества перед массовым наполнением A2–C1.

Этап 3 сохранил исходные 292 элемента и дневную нагрузку, но сделал A1
эталоном глубины. `content:check` требует полный A1-шаблон для каждого вида
контента и проверяет двусторонние связи `lessonIds`/`exerciseIds`. Два
listening-элемента теперь ведут на конкретные проверенные материалы, поэтому
их защищённые URL получили `revision: 2`. Новые поля отображаются в теории и
практике; browser acceptance охватил каждый затронутый тип карточки.

### Этап 4. Расширение A2–C1

- [x] Применить утверждённый шаблон ко всем существующим элементам A2.
- [x] Применить утверждённый шаблон ко всем существующим элементам B2.
- [x] Применить утверждённый шаблон ко всем существующим элементам C1.
- [x] Сначала углубить существующие элементы, затем добавлять новые.
- [ ] Пополнять в первую очередь разделы с самой короткой ротацией:
      listening, переводы, чтение и спряжения. — A1 закрыт, остаются A2–C1.
- [ ] После каждого уровня проверять связи тем и реальный горизонт повторения.
      — выполнено для A1.

Вертикальный срез A2 сохранил 55 существующих учебных A2-элементов и дневную
нагрузку; три placement-вопроса остаются в отдельном этапе 5.
Все виды контента получили тот же полный шаблон, что и A1; две общие
listening-ссылки заменены конкретными материалами VideoEle уровня A2 и получили
`revision: 2`. `content:check` теперь требует полный шаблон и проверяет
двусторонние theory/grammar-связи для A1 и A2. Новые элементы на этом шаге не
добавлялись: сначала закрыто углубление существующего банка. Browser acceptance
на A2-профиле подтвердил theory и все затронутые типы practice-карточек без
runtime error overlay.

Вертикальный срез B1 сохранил 55 существующих учебных B1-элементов и дневную
нагрузку; три placement-вопроса остаются в отдельном этапе 5. Generic
playlist/channel в listening заменены конкретными уроками VideoEle «Un día en
la playa» и «No se puede», а все виды контента получили полный учебный шаблон.
`content:check` теперь требует его и проверяет двусторонние
theory/grammar-связи для A1–B1. Новые элементы не добавлялись. Browser
acceptance на B1-профиле подтвердил theory, grammar, vocabulary, conjugations,
reading, listening и оба направления перевода без runtime error overlay.

Вертикальный срез B2 сохранил 55 существующих учебных B2-элементов и дневную
нагрузку; три placement-вопроса остаются в отдельном этапе 5. Обе
listening-карточки вели на канал и плейлист и заменены конкретными уроками
VideoEle «Si lo hubieran sabido…» и «El viaje de mis sueños» с проверенными
длительностями и заданиями по транскрипции, поэтому их защищённые URL получили
`revision: 2`. Все виды контента получили полный учебный шаблон, а
`content:check` теперь требует его и проверяет двусторонние
theory/grammar-связи для A1–B2. Новые элементы не добавлялись. Seed проверен на
существующей базе: 292 активные записи, повторный прогон даёт идентичное
состояние таблицы `ContentItem` (совпадение контрольной суммы). Browser
acceptance на B2-профиле в этом срезе не выполнялся: рендер не менялся, а
дневной план B2-профиля через `/api/day` возвращает все затронутые типы карточек
с новыми полями.

Вертикальный срез C1 закрыл шаблонную часть этапа 4: 57 существующих учебных
C1-элементов, включая 22 grammar-упражнения, получили полный шаблон без
изменения дневной нагрузки; три placement-вопроса остаются в этапе 5. Обе
listening-карточки вели на каналы Hola Spanish и SpanishPod101 и заменены
конкретными эпизодами Radio Ambulante «El lazareto» (Колумбия, 41 мин) и «A seis
mil metros de altura» (Аргентина, 46 мин); длительности сверены по каталогу
подкаста, вопросы и summary написаны по публичным расшифровкам, поэтому
защищённые URL получили `revision: 2`. `content:check` теперь требует полный
шаблон и двусторонние theory/grammar-связи для всех уровней A1–C1. Seed проверен
на существующей базе: 292 активные записи, повторный прогон даёт идентичную
контрольную сумму `ContentItem`. Рендер не менялся, поэтому отдельный browser
acceptance не выполнялся.

#### Расширение банка A1 (короткая ротация)

Первый срез фазы расширения этапа 4: A1 доведён по разделам с самой короткой
ротацией. Добавлено 43 новых учебных элемента (292 → 335), дневная нагрузка и
существующие ID не тронуты:

| Раздел        | Было → стало | Новые ID     | Горизонт    |
| ------------- | ------------ | ------------ | ----------- |
| Спряжения     | 6 → 30       | `cj-a1-7…30` | 0.3д → 1.5д |
| Аудирование   | 2 → 8        | `v11…v16`    | 1.0д → 4.0д |
| Чтение        | 3 → 8        | `r-a1-4…8`   | 3.0д → 8.0д |
| Перевод ES→RU | 2 → 6        | `tf-a1-3…6`  | 2.0д → 6.0д |
| Перевод RU→ES | 2 → 6        | `ta-a1-3…6`  | 2.0д → 6.0д |

Все новые элементы реализуют полный учебный шаблон, который `content:check`
требует для A1. Спряжения покрывают presente indicativo: остальные частотные
правильные глаголы, чередования корня e→ie, o→ue, u→ue, e→i и глаголы с
нерегулярной формой yo (`hago`, `pongo`, `salgo`, `sé`, `veo`, `conozco`,
`estoy`, `vengo`). 30 карточек — минимум, при котором ротация реально существует:
дневная выборка берёт 20 спряжений, поэтому при банке в 20 генератор ежедневно
возвращал бы весь пул.

Шесть новых listening-карточек — уроки VideoEle уровня A1 (`La comida`,
`Mi casa`, `En mi barrio hay…`, `Medios de transporte`, `Voy al instituto`,
`¡Buen fin de semana!`). Каждый URL проверен (HTTP 200), длительность взята из
фактической длины видео на YouTube, вопросы и summary написаны по официальным
транскрипциям VideoEle; `lastVerifiedAt` = 2026-07-23. Listening продолжает
сквозную нумерацию `v1…v16`, а не пер-уровневую.

Seed проверен на существующей базе: 335 активных записей, повторный прогон даёт
идентичную контрольную сумму `ContentItem` (сортировка по `(kind, sourceId)`;
глобальный `position` не уникален — он назначается внутри вида контента).
Рендер-шейп не менялся: новые элементы используют тот же A1-шаблон, принятый и
проверенный браузером в этапе 3, поэтому отдельный browser acceptance не
выполнялся.

### Этап 5. Placement и ревизии

- [ ] Расширить и вычитать placement-банк.
- [ ] Отдельно решить, сохраняются ли 15 фиксированных вопросов или выбираются
      15 вопросов из расширенного банка.
- [ ] Реализовать окончательную стратегию ревизий контента и сохранения
      исторического вида учебного дня.

## Обязательные проверки

`content:check` должен завершаться ошибкой при любом из условий:

- повторяющийся ID;
- неизвестный уровень или topic;
- пустое обязательное поле;
- неправильное число форм спряжения;
- MCQ-ответ отсутствует среди вариантов после нормализации;
- обязательный `contextTarget` отсутствует в контексте;
- упражнение с несколькими пропусками не имеет явного `solvedExample`;
- theory topic не имеет упражнений;
- упражнение с обязательной theory-связью осталось orphan;
- listening URL невалиден;
- защищённые смысловые поля существующего ID изменены без новой ревизии;
- каталог не достигает зафиксированного минимального объёма.

После каждого законченного среза выполняются:

```bash
npm run content:check
npm run db:seed
npm run verify
npm run docker:config
```

Seed нужно запускать дважды и подтверждать одинаковый результат. Для изменений
рендера требуется browser acceptance всех затронутых типов карточек.

## Критерий завершения инициативы

- Каталог разделён по уровню и типу, монолит больше не редактируется вручную.
- Все элементы проходят runtime-валидацию до обращения к БД.
- P0-дефекты исправлены.
- Каждый тип контента имеет полноценный учебный шаблон.
- A1–C1 наполнены до согласованных ориентиров либо имеют документированное
  исключение.
- Адаптивное повторение вызывается прогрессом пользователя, а не постоянной
  нехваткой материалов.
- Старые попытки и дни не получают несовместимый новый prompt/answer.
- Документы соответствуют реальному формату каталога и seed-процессу.

## Точка старта следующей сессии

Этапы 1–3, шаблонная часть этапа 4 и расширение A1 закрыты: 335 элементов
находятся в модульном каталоге, P0-дефекты исправлены, все уровни A1–C1
реализуют и отображают полный учебный шаблон, а A1 доведён по короткоротационным
разделам (спряжения 30, listening 8, чтение 8, оба перевода по 6).
`content:check` требует шаблон для каждого уровня. Следующая работа — повторить
это расширение для A2, затем B1, B2, C1: добавлять новые элементы по ориентирам
объёма, начиная с разделов с самой короткой ротацией (listening, оба перевода,
чтение, спряжения), с проверкой реального горизонта повторения после каждого
уровня. Listening требует реальных проверенных ссылок (для A2–B2 уже есть уроки
VideoEle, для C1 — эпизоды Radio Ambulante; новые ссылки проверять WebFetch перед
записью и ставить `lastVerifiedAt`). Отдельно остаётся этап 5: расширение
placement-банка и окончательная стратегия ревизий.

После расширения каждого раздела нужно перегенерировать manifest
(`npm run content:manifest`), потому что новые ID обязаны присутствовать в
protected-fields manifest.
